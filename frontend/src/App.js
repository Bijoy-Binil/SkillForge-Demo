import ResumeBuilder from './components/ResumeBuilder';

function App() {
    const menuItems = [
        {
            title: 'Resume Builder',
            path: '/resume-builder',
            component: ResumeBuilder,
            icon: 'file-text'
        },
    ];
} 